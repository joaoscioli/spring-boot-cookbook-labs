spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/product_catalog}
    username: ${SPRING_DATASOURCE_USERNAME:product_catalog}
    password: ${SPRING_DATASOURCE_PASSWORD:product_catalog}
    driver-class-name: org.postgresql.Driver
